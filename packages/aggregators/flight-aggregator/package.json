{
  "name": "@triggerr/flight-aggregator",
  "version": "0.1.0",
  "private": true,
  "description": "A service for aggregating flight data from multiple sources for the triggerr platform.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@triggerr/shared": "workspace:*",
    "@triggerr/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
