{
  "name": "@triggerr/weather-aggregator",
  "version": "0.1.0",
  "private": true,
  "description": "A service for collecting and analyzing weather data from multiple sources for parametric insurance products.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@triggerr/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
