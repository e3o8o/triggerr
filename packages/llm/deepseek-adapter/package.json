{
  "name": "@triggerr/deepseek-adapter",
  "version": "1.0.0",
  "private": true,
  "description": "An adapter to interact with the DeepSeek LLM API, implementing the generic ILLMClient interface.",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@triggerr/llm-interface": "*"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0"
  }
}
