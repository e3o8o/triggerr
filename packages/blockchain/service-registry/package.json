{
  "name": "@triggerr/service-registry",
  "version": "1.0.0",
  "private": true,
  "description": "A runtime registry for managing and providing access to multiple, concurrent blockchain service adapters.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo dist"
  },
  "dependencies": {
    "@triggerr/blockchain-interface": "*",
    "@triggerr/paygo-adapter": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
