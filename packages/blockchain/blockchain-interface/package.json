{
  "name": "@triggerr/blockchain-interface",
  "version": "1.0.0",
  "private": true,
  "description": "Defines the generic, platform-agnostic interfaces and models for blockchain interactions within the triggerr platform.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "viem": "^2.0.0"
  }
}
